# Fetch dependencies from local cache whenever possible
prefer-offline=true

# Disable pnpm v10 background supply-chain policy checks on every script run
verify-deps-before-run=false

# Optimize download concurrency and timeouts
fetch-retries=5
fetch-retry-maxtimeout=60000